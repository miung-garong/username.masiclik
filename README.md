# Instagram Username Generator by VINS

Single-file GitHub Pages tool.

## Deploy
1. Upload `index.html` to the repository.
2. Open GitHub Pages settings.
3. Set the deployment source to the repository/branch containing `index.html`.
4. Open the generated Pages URL.

No server or API is required.
